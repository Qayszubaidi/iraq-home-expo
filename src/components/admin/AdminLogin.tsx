"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { signIn } from "@/lib/cms/adminClient";
export default function AdminLogin(){const router=useRouter();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{await signIn(email,password);router.replace("/admin")}catch(err:any){setError(err.message)}finally{setBusy(false)}}return <main className="adminLoginPage"><form className="adminLoginCard" onSubmit={submit}><div className="adminLoginMark">IH</div><span>Private website control</span><h1>Iraq Home Expo Admin</h1><p>Sign in with your authorized Supabase administrator account.</p><label><span>Email</span><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></label><label><span>Password</span><input type="password" required value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/></label>{error&&<div className="adminLoginError">{error}</div>}<button disabled={busy}>{busy?"Signing in…":"Sign in"}</button><a href="/">← Back to website</a></form></main>}
